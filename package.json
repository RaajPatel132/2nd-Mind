{
  "name": "secondmind-repo",
  "private": true,
  "description": "Repo-level developer tooling: git hooks (husky) and the commit message standard (commitlint).",
  "type": "module",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.3",
    "@commitlint/config-conventional": "^21.2.3",
    "husky": "^9.1.7"
  }
}
